[pytest]
markers =
    engine: tests that launch a real Stockfish binary (opt-in; run with -m engine)
    live_api: tests that hit a real upstream HTTP API (opt-in)
addopts = -m "not engine and not live_api"
